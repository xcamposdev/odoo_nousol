<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="export_prestashop_product_category" model="base.automation">
            <field name="name">Categoría De Productos</field>
            <field name="model_id" ref="product.model_product_category"/>
            <field name="active" eval="True"/>
            <field name="trigger">on_create_or_write</field>
            <field name="state">code</field>
            <field name="filter_domain">[('prestashop_category_id', '=', False)]</field>
            <!-- <field name="filter_pre_domain">[('state', '=', 'open')]</field> -->
            <field name="code">
                action = env['product.category'].search([('id','=',record.id)]).categories_sincronize()
            </field>
        </record>
    </data>
</odoo>